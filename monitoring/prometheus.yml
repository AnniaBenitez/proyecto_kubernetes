global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/rules.yml

scrape_configs:
  - job_name: backend
    static_configs:
      - targets:
          - backend:3000

  - job_name: "node"
    static_configs:
      - targets: ["node-exporter:9100"]
